# 贡献指南 (Contributing to NachoBot)

感谢你对 **NachoBot** 项目的关注！我们非常欢迎并赞赏各类贡献，无论是修复 Bug、完善文档、提出新特性，还是开发新的平台适配器。

为了确保项目的代码质量和开发效率，请在参与贡献前花几分钟阅读以下指南。

---

## 行为准则

参与本项目的开发意味着你同意遵守友善、互相尊重的沟通原则。请在 Issue 和 Pull Request 中保持理性和客观，共同维护一个良好的开源社区氛围。

---

## 如何参与贡献

### 报告 Bug
如果你在使用过程中发现了 Bug，请首先确认是否使用的是最新版本。如果是，请提交一个 Issue 并包含以下信息：
* **环境信息**：Python 版本、操作系统、以及运行环境。
* **复现步骤**：清晰、具体地描述如何触发该 Bug。
* **预期行为**与**实际行为**的对比。
* **日志/报错信息**：粘贴相关的终端输出或日志（请注意隐藏敏感的 Token 或 API Key）。

### 提出功能建议
如果你有好的想法（例如支持新的平台、优化 LLM 的响应速度、增加有趣的互动功能等），欢迎提交 Issue：
* 请详细描述该功能的使用场景和核心价值。
* 尽可能提供你设想的实现思路或交互流程。

### 提交代码 (Pull Request)
1. **Fork** 本仓库到你的个人 GitHub 账号。
2. 基于主分支（`main` 或 `master`）创建一个新的功能分支：
   ```
   git checkout -b feature/your-feature-name
   # 或者修复 Bug 分支
   git checkout -b fix/your-bug-name
   ```

3. 在本地进行开发和测试。
4. 确保代码风格符合 Python 规范（推荐使用 Black 或 Flake8 进行格式化），无明显逻辑漏洞。
5. 提交你的修改并推送到你的 Fork 仓库。
6. 在本仓库发起一个 Pull Request (PR)，并详细描述你的修改内容。

---

## 开发指南与技术栈

NachoBot 是一个基于 **Python** 开发的、面向多平台的 AI 角色机器人系统。

### 开发环境准备

* **Python 版本**： Python 3.11 或更高版本。

### 核心技术规范

1. **异步编程 (Asynchronous)**：由于本项目涉及多平台的消息监听与并发网络请求，核心逻辑均采用 `asyncio` 实现。编写代码时请严格遵守异步规范，避免在异步函数中调用阻塞（blocking）操作。
2. **多平台适配器 (Multi-platform Adapters)**：NachoBot 目前支持或计划支持 **Bilibili**、**Discord** 和 **QQ** 等平台。新增平台或扩展现有平台功能时，请遵循项目中定义的 Adapter 基类或接口标准，确保架构的解耦与一致性。
3. **多模态大模型集成**：项目支持本地大模型接入。在修改提示词（Prompt）或上下文管理逻辑时，请注意处理好长文本的截断与异步流式（Stream）输出的稳定性。

### Bot 人设与言行规范

  用户在使用默认角色设定 NachoBot 时，需了解几点：
  NachoBot 具有特殊的角色属性。在为 Bot 编写内置 Prompt、预设回复或核心人格逻辑时，请务必遵守以下原则

* **机器人身份认知**：NachoBot 在设定上应当明确承认自己是一个“人工智能/机器人/数字生命”，**不得**直接宣称自己是现实中的某个真实个体或和现实中的任意人物存在物理上的任何关系。
* **角色独立性**：NachoBot 的设定未基于任何现实人物创作，如有雷同，纯属巧合。

---

## Git 提交规范

我们推荐使用结构化的 Commit Message，以便于自动生成 Changelog 和维护历史记录。格式建议如下：

```
<type>(<scope>): <subject>
```

常见的 `<type>` 类型包括：

* `feat`: 新增功能 (Feature)
* `fix`: 修复 Bug
* `docs`: 仅修改文档 (Documentation)
* `style`: 代码格式调整（不影响运行逻辑的空格、分号等变动）
* `refactor`: 代码重构（既不修复 Bug 也不增加新功能）
* `test`: 增加或修改测试用例
* `chore`: 构建过程或辅助工具的变动（如依赖更新、配置调整）

**示例**：

* `feat(adapter): add support for Discord stream response`

---

再次感谢你为 NachoBot 注入活力！如果你有任何疑问，可以在 Issue 中直接提及项目管理员。

```
